# unayfield
unayfield is a library that is essentially a fork of Rayfield. However, unayfield differs in that it does not have external analytical queries and does not collect analytics, and is only a library for the interface, while Rayfield essentially collects information.
